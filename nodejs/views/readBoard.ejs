<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        body {
            display: flex;
            background-color: rgb(233, 233, 233);
        }

        #main {
            background-color: white;
            width: 1000px;
            height: 600px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin: 150px auto;
            position: relative;

        }

        .header {
            position: absolute;
            width: inherit;
            height: 80px;
            background-color: #90e0ef;
            top: 0px;
            display: flex;

        }

        .header span {
            display: flex;
            font-size: large;
            align-items: center;
            justify-content: center;
        }

        nav {
            background-color: #00b4d8;
            display: flex;
            position: absolute;
            height: 80px;
            right: 0;
            align-items: center;

        }

        nav a {
            color: white;
            box-sizing: border-box;
            padding: 20px;
            text-decoration: none;
            text-align: center;
        }

        nav a:hover {
            background-color: #0077b6;
            color: white;
            box-sizing: inherit;
        }

        .container {
            width: 1100px;
            margin: 0 auto;
        }

        .title [type=text] {
            position: absolute;
            top: 200px;
            right: 230px;
            width: 500px;
            height: 40px;
            border: 1px solid #00b4d8;
            outline: #00b4d8;
            box-sizing: border-box;
            border-radius: 15px;
            padding-left: 15px;

        }

        textarea {
            position: absolute;
            top: 250px;
            left: 270px;
            margin: 10px auto;
            min-width: 500px;
            max-width: 500px;
            min-height: 300px;
            max-height: 300px;
            border: 1px solid #00b4d8;
            outline: #00b4d8;
            border-radius: 15px;
            box-sizing: border-box;
            padding: 20px;
        }

        h1 {
            position: absolute;
            top: 117px;
            left: 442px;
            margin: 0px;
            width: 160px;
            height: 45px;
            text-align: center;
            font-family: 'Times New Roman', Times, serif;
            font-weight: 100;
        }

        .side {
            position: absolute;
            top: 500px;
            right: 100px;
        }

        .side [type=button] {
            width: 100px;
            height: 50px;
            border: 0px solid #00b4d8;
            border-radius: 15px;
            background-color: #bde0fe;
        }

        .side [type=button]:active {
            background-color: #a2d2ff;
        }
    </style>

</head>

<body>

    <div id="main">
        <div class="header">
            <span id=flag1>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;게임 데이터 아트: LOL 시각적 표현의 세계 </span>
            <nav>
                <a href="/board">Main</a>
                <a href="/profile">My Page</a>
                <a href="/">Logout</a>
            </nav>

        </div>
        <form >
            <h1>Read Only</h1>
            <div class="title">
                <input type="text" placeholder="<%=data[0].b_title %>" disabled>
            </div>
            <div class="txt">
                <textarea name="content" placeholder="<%=data[0].b_content %>" disabled></textarea>
            </div>
            <div class="side">
                <input type="button" value="DELETE" onclick="deleteCheck()">
            </div>
        </form>


    </div>
    </form>

    <script>

        function deleteCheck() {

            if(window.confirm("정말 삭제하시겠습니까?")){
               
                location.href = "/deleteBoard?num=<%=data[0].b_cnt %>"
                return;
            }else{
                alert("취소하셨습니다.")
            }

        }


        const a = document.getElementById('flag1')
        a.append(getCookie('id'), '님 환영합니다.!!');

        function getCookie(name) { //쿠키값확인용
            var value = document.cookie.match('(^|;) ?' + name + '=([^;]*)(;|$)');
            return value ? unescape(value[2]) : null;
        };
    </script>
</body>

</html>